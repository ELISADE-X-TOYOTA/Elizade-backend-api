fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
# Pinned explicitly, not left to arrive via fastapi. An unpinned transitive
# pydantic is how production ended up on a different version from local and
# started warning that `Field(alias=...)` on a union had no effect — a silent
# break for every camelCase request body.
pydantic==2.13.4
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0
openpyxl==3.1.5
httpx>=0.27
boto3>=1.34
